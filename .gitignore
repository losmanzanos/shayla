node_modules/
__pycache__/
*.pyc
.env
.env.local

# tina-lock.json MUST be committed — TinaCloud reads it to index the content
# schema, and without it the editor never loads. Only the generated client
# code is ignored.
tina/__generated__/*
!tina/__generated__/.gitkeep

.DS_Store
dist/
